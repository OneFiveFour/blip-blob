<resources>
    <!-- Base application theme. -->
    <style name="Theme.SessionTimer" parent="Theme.Material3.DayNight">
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
    </style>

    <style name="Theme.SessionTimer.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.SessionTimer.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.SessionTimer.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>